name: docker-compose-actions-workflow
on: push
jobs:
  test:
    timeout-minutes: 1
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Docker build
        run: |
          DOCKER_BUILDKIT=1 docker image build --no-cache . -f docker/test.Dockerfile -t testcontainer